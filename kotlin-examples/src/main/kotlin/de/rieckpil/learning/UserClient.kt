package de.rieckpil.learning

import java.util.*

class UserClient {
  fun fetchInformation(username: String) = UUID.randomUUID().toString()
  fun fetchDetails(username: String) = "Details about $username"
}
