package de.rieckpil.learning.fpbook.propertytesting

fun main() {

}


/**
 * Properties:
 *
 * - reversing should be the same sum
 * - empty list should have sum 0
 * - only negative values should have negative sum
 * - all elements have same value -> sum = value * length
 */
fun sum(list: List<Int>): Int = 0

/**
 * Properties:
 *
 * - empty list should have max 0?
 * - reordering should result in the same max
 * - all elements are same value -> max is value
 */
fun findMax(list: List<Int>): Int = 0
